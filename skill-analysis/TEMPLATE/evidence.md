<!--
TEMPLATE — verbatim transcript excerpts. Quote the agents' own commands and final
messages; do not paraphrase. Contrast the arms on the hardest scenario — that
contrast is the proof a reader clicks through to. Delete these comments in a real
study.
-->

# Evidence — `<skill-name>`

## Scenario 3 (the hard case): how each arm handled it

### No skill

> _verbatim command(s) the bare agent ran, and its final message — showing the
> wrong move or the stall._

### Skill (discovered)

> _verbatim — did it surface the skill from a plain request, and what did it do?_

### Skill (told)

> _verbatim — the skill's ceiling when named._

## Notes

_Anything a reader needs to read the excerpts fairly — run tags (so each excerpt
ties back to a `data.csv` row), what was trimmed, what was stubbed._
